version: '3.8'

# This override file should be used when running this service in isolation
# to provide dependencies and expose ports for local testing

services:
  ffc-grants-productivity-web:
    build:
      context: .
      target: development
    depends_on:
      - redis
    image: ffc-grants-productivity-web-development
    container_name: ffc-grants-productivity-web-development
    ports:
      - "3000:3000"
      - "9229:9229"
    volumes:
      - ./app/:/home/node/app/
      - ./package.json:/home/node/package.json

  redis:
    image: redis:4.0.14
    volumes:
      - redis_data:/data
    ports:
      - "6380:6379"

volumes:
  redis_data:
