{{- include "ffc-helm-library.azure-ingress" (list . "ffc-grants-productivity-web.ingress") -}}
{{- define "ffc-grants-productivity-web.ingress" -}}
{{- if not .Values.pr -}}
metadata:
  annotations:
    nginx.org/mergeable-ingress-type: minion
{{- end -}}
{{- if .Values.pr -}}
spec:
  rules:
  - host: {{ .Values.name }}-{{ .Values.pr }}.{{ .Values.ingress.server }}
    http:
      paths:
      - path: {{ .Values.ingress.path | default "/" | quote }}
        backend:
          serviceName: {{ .Values.name | quote }}
          servicePort: {{ .Values.service.port }}
{{- end -}}
{{- end -}}
